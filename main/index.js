// This file handles all the weird p5 stuff.
import { setup, draw, preload, mousePressed } from "./sketch.js";

// If you add another p5-managed function, add it here!
window.setup = setup;
window.draw = draw;
window.preload = preload;
window.mousePressed = mousePressed;
